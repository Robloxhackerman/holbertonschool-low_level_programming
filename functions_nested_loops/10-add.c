#include <stdio.h>
#include "main.h"

/**
 * add - add 2 ints
 *
 * @p1: value to be checked
 * @p2: value to be checked
 *
 * Return: return the result of p1 + p2
 */
int add(int p1, int p2)
{
	int PEPE;

	PEPE = p1 + p2;
	return (PEPE);
}
