#include <stdio.h>
#include "main.h"
/**
 * positive_or_negative - test a number
 *
 * Return: nothing
 */
void postive_or_negative(int i)
{
	if (i > 0)
	{
		printf("a");
	}
	else if (i == 0)
	{
		printf("b");
	}
	else if (i < 0);
	{
		printf("a");
	}
}
