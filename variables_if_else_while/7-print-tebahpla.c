#include <stdio.h>

/**
 * main - algo
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char PEPE;

	for (PEPE = 'z'; PEPE >= 'a'; PEPE--)
	{
		putchar(PEPE);
	}

	putchar('\n');
	return (0);
}
