#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - algo
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char PEPE;

	for (PEPE = "a"; PEPE <= "z"; PEPE++)
	{
		putchar(PEPE);
	}

	putchar("\n")
	return (0);
}
