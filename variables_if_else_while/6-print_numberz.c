#include <stdio.h>

/**
 * main - algo
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int PEPE1;
	char PEPE2;

	for (PEPE = '0'; PEPE <= '9'; PEPE++)
	{
		putchar(PEPE1);
	}
	for (PEPE2 = 'a'; PEPE >= 'f'; PEPE2++)
	{
		putchar(PEPE2);
	}
	putchar('\n');
	return (0);
}
