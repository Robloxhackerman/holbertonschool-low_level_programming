#include "main.h"
#include <stdio.h>

/**
 * main - prnits name
 *
 * @argc: a
 * @argv:
 *
 * Return: Always 0 (success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
